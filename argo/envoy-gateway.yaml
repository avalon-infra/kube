apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: envoye-gateway
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    chart: eg
    repoURL: docker.io/envoyproxy/gateway-helm
    targetRevision: v1.7.0
  destination:
    name: in-cluster
    namespace: envoye-gateway
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      selfHeal: true
      prune: true
