apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wiki-tls
spec:
  secretName: wiki-tls
  issuerRef:
    name: prod-cluster-issuer
    kind: ClusterIssuer
  commonName: wiki.matiix310.dev
  dnsNames:
    - "wiki.matiix310.dev"
