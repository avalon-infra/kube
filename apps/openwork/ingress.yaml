apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: openwork-route
spec:
  entryPoints:
    - websecure
  tls:
    secretName: openwork-tls
  routes:
    - match: Host(`openwork.matiix310.dev`)
      kind: Rule
      services:
        - name: openwork-openwork-ee-den-web
          port: http
