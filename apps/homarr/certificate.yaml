apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homarr-tls
spec:
  secretName: homarr-tls
  issuerRef:
    name: prod-cluster-issuer
    kind: ClusterIssuer
  commonName: dashboard.matiix310.dev
  dnsNames:
    - "dashboard.matiix310.dev"
