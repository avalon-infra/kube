apiVersion: postgresql.cnpg.io/v1
kind: DatabaseRole
metadata:
  name: authentik
spec:
  cluster:
    name: pg-cluster
  name: authentik
  ensure: present
  login: true
  passwordSecret:
    name: authentik-pg
  comment: Application role for authentik
