apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: authentik-tls
spec:
  secretName: authentik-tls
  issuerRef:
    name: prod-cluster-issuer
    kind: ClusterIssuer
  commonName: auth.matiix310.dev
  dnsNames:
    - "auth.matiix310.dev"
