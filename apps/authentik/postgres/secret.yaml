apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: postgres-auth
spec:
  path: kvv2/authentik
  type: Opaque
  templates:
    POSTGRES_PASSWORD: "{% .Secrets.POSTGRES_PASSWORD %}"
