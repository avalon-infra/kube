apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tino-tls
spec:
  secretName: tino-tls
  issuerRef:
    name: prod-cluster-issuer
    kind: ClusterIssuer
  commonName: tino.matiix310.dev
  dnsNames:
    - "tino.matiix310.dev"
